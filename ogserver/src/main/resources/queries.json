{
  "queries":
  [
  	{"id" : "user_profile.insert", 
  	 "type" : "insert",
  	 "table": "user_profile",
  	 "query": "insert into user_profile (id, user_id, profile, hash, salt) values (?, ?, ?, ?, ?)",
  	 "params": ["id", "user_id", "profile", "hash", "salt"]
  	 },

  	{"id" : "user_profile.select.id", 
  	 "type" : "select",
  	 "query": "select user_id, hash, salt from user_profile where user_id = ?",
  	 "params": ["user_id"]
  	 }
  ]
}

